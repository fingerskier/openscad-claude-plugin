# openscad-claude-plugin
Claude-code plugin for generating parametric models

## Requirements
* OpenSCAD pre-installed

## Features
* Render an image from a .scad
* Generate G-code
* Generate Mesh
* ?host an OpenSCAD instance for live viewer?
